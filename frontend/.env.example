VITE_API_URL=http://localhost:5000/api
# Optional — defaults to same origin (Vite proxies /socket.io in dev)
# VITE_SOCKET_URL=http://localhost:5000

# Production build uses .env.production (Render URLs). Netlify env vars also work if you build on Netlify.
