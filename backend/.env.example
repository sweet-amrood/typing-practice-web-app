PORT=5000
# Use this same URI in MongoDB Compass (Connect → New Connection)
MONGODB_URI=mongodb://localhost:27017/typing-practice
NODE_ENV=development
CLIENT_URL=http://localhost:5173
# Optional extra origins (comma-separated), e.g. preview deploys
# CLIENT_URLS=https://branch--typing-learning.netlify.app
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d
